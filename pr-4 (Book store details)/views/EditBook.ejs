<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General styling for the body */
        body {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            font-family: Arial, sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        /* Container styling */
        .container {
            background: #fff;
            color: #333;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            padding: 25px 35px;
            max-width: 600px;
            width: 100%;
            animation: fadeIn 0.5s ease-in-out;
        }

        /* Title styling */
        .d-flex h1 {
            margin: 0;
            font-size: 26px;
            font-weight: bold;
            color: #333;
            text-align: center;
        }

        /* "View" button styling */
        .btn-dark a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }

        .btn-dark:hover a {
            color: #ddd;
        }

        .btn-dark {
            border-radius: 20px;
            padding: 8px 16px;
            font-size: 14px;
            transition: background 0.3s ease, transform 0.2s ease;
        }

        .btn-dark:hover {
            background: #444;
            transform: translateY(-2px);
        }

        /* Form input styling */
        .form-control {
            border-radius: 25px;
            border: 1px solid #ccc;
            padding: 12px 15px;
            margin-bottom: 20px;
            font-size: 14px;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-control:focus {
            border-color: #6a11cb;
            box-shadow: 0 0 8px rgba(106, 17, 203, 0.5);
        }

        /* Form label styling */
        .form-label {
            font-weight: bold;
            font-size: 14px;
            margin-bottom: 5px;
            display: block;
        }

        /* Submit button styling */
        .btn-primary {
            background: linear-gradient(90deg, #6a11cb, #2575fc);
            border: none;
            border-radius: 25px;
            padding: 12px 18px;
            font-size: 16px;
            font-weight: bold;
            color: #fff;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        /* Image preview styling */
        .image-preview {
            display: block;
            margin: 10px 0;
            max-width: 200px;
            height: 150px;
            border-radius: 10px;
            border: 1px solid #ddd;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            object-fit: cover;
        }

    </style>

</head>

<body>

    <div class="container w-50 mx-auto">
        <div class="d-flex justify-content-center py-3">
            <h1 class="text-center">Edit Book</h1>
            <button class="btn btn-dark ms-4" style="width: 80px;">
                <a href="/view">View</a>
            </button>
        </div>
        <div class="border rounded">
            <form action="/updatetDetail" method="post" enctype="multipart/form-data" class="p-4">
                <input type="hidden" name="editid" value="<%= single.id %>">
                <div class="mb-3">
                    <label for="bookName" class="form-label">Book Name:</label>
                    <input type="text" class="form-control" name="name" value="<%= single.name %>" id="bookName">
                </div>
                <div class="mb-3">
                    <label for="bookPrice" class="form-label">Book Price:</label>
                    <input type="text" class="form-control" name="price" value="<%= single.price %>" id="bookPrice">
                </div>
                <div class="mb-3">
                    <label for="bookPages" class="form-label">Book Pages:</label>
                    <input type="number" class="form-control" name="pages" value="<%= single.pages %>" id="bookPages">
                </div>
                <div class="mb-3">
                    <label for="bookAuthor" class="form-label">Book Author:</label>
                    <input type="text" class="form-control" name="author" value="<%= single.author %>" id="bookAuthor">
                </div>
                <div class="mb-3">
                    <label for="bookImage" class="form-label">Book Image:</label>
                    <input type="file" class="form-control" name="image" id="bookImage">
                    <img src="<%= single.image %>" alt="Book Image" class="image-preview">
                </div>
                <button type="submit" class="btn btn-primary">Update Book</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>